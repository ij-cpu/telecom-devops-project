apiVersion: v1
kind: Service
metadata:
  name: telecom-service
spec:
  type: NodePort
  selector:
    app: telecom
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007

